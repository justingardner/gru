% AuthLoginOutput
%
% AuthLoginOutput Properties:
%    token 
%
% AuthLoginOutput Methods:
%    toJson - Convert the object to a Map that can be encoded to json
%    struct - Convert the object to a struct
    classdef AuthLoginOutput < flywheel.ModelBase
    % NOTE: This file is auto generated by the swagger code generator program.
    % Do not edit the file manually.
    properties (Constant)
        propertyMap = containers.Map({ 'token' }, ...
            { 'token' });
    end
    properties(Dependent)
        token
    end
    methods
        function obj = AuthLoginOutput(varargin)
            obj@flywheel.ModelBase(flywheel.model.AuthLoginOutput.propertyMap);

            % Allow empty object creation
            if length(varargin)
                p = inputParser;
                addParameter(p, 'token', []);

                parse(p, varargin{:});

                if ~isempty(p.Results.token)
                    obj.props_('token') = p.Results.token;
                end
            end
        end
        function result = get.token(obj)
            if ismethod(obj, 'get_token')
                result = obj.get_token();
            else
                if isKey(obj.props_, 'token')
                    result = obj.props_('token');
                else
                    result = [];
                end
            end
        end
        function obj = set.token(obj, value)
            obj.props_('token') = value;
        end
        function result = toJson(obj)
            result = containers.Map;
            if isKey(obj.props_, 'token')
                result('token') = flywheel.ModelBase.serializeValue(obj.props_('token'), 'char');
            end
        end
        function result = struct(obj)
            result = struct;

            if isKey(obj.props_, 'token')
                result.token = obj.props_('token');
            else
                result.token = [];
            end
        end
        function result = returnValue(obj)
            result = obj;
        end
    end
    methods(Access = protected)
        function prpgrp = getPropertyGroups(obj)
            if ~isscalar(obj)
                prpgrp = getPropertyGroups@matlab.mixin.CustomDisplay(obj);
            else
                propList = struct;
                if isKey(obj.props_, 'token')
                    propList.token = obj.props_('token');
                else
                    propList.token = [];
                end
                prpgrp = matlab.mixin.util.PropertyGroup(propList);
            end
        end
    end
    methods(Static)
        function obj = fromJson(json, context)
            obj =  flywheel.model.AuthLoginOutput;
            if isfield(json, 'token')
                obj.props_('token') = flywheel.ModelBase.deserializeValue(json.token, 'char');
            end
            if isprop(obj, 'context_')
                obj.setContext_(context);
            end
        end
        function obj = ensureIsInstance(obj)
            if ~isempty(obj)
                % Realistically, we only convert structs
                if ~isa(obj, 'flywheel.model.AuthLoginOutput')
                    obj = flywheel.model.AuthLoginOutput(obj);
                end
                if isKey(obj.props_, 'token')
                end
            end
        end
    end
end
